import { enumToObject } from '../helpers';

export enum IMPDATA_SOURCES_ENUM {
  diaryComp = 'diaryComp',
}

export enum IMPDATA_STATUS_ENUM {
	waiting = 'waiting',
  working = 'working',
  ready = 'ready',
  error = 'error',
}
